"use client";

export function Auth() {
  return null; // Don't render anything since we're using the env variable directly
}

export function AuthDialog() {
  return null; // Don't render the dialog since we're using the env variable directly
}
